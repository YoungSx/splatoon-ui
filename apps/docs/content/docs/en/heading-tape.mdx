export const metadata = {
  title: 'HeadingTape',
  description: 'Tape-backed heading treatment with optional sticker decorations and safe-area sizing.',
  category: 'typography',
  example: 'heading-tape',
}

## Usage

Use HeadingTape for section titles that need a taped label treatment.

## Design Notes

- Use `size="compact"` inside tight surfaces.
- Disable decorations when neighboring content is already visually dense.
