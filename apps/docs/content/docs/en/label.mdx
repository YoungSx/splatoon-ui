export const metadata = {
  title: 'Label',
  description: 'Compact form label primitive for native controls and Splatoon UI inputs.',
  category: 'forms',
  example: 'label',
}

## Usage

Use Label to name inputs, selects, radios, switches, and other form controls.

## Design Notes

- Prefer explicit `htmlFor` when labeling a single control.
- Let disabled state come from the parent group when controls share state.
