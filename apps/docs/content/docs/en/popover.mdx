export const metadata = {
  title: 'Popover',
  description: 'Contextual floating surface with trigger, positioning, title, and description parts.',
  category: 'feedback',
  example: 'popover',
}

## Usage

Use Popover for lightweight contextual content that should not interrupt the current workflow.

## Design Notes

- Use Dialog for blocking decisions.
- Keep popover content concise enough to dismiss quickly.
