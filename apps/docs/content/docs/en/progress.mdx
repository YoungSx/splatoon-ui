export const metadata = {
  title: 'Progress',
  description: 'Skewed progress meter with splattered fill, sizes, and color variants.',
  category: 'feedback',
  example: 'progress',
}

## Usage

Use Progress for known completion states. The component clamps values and keeps the visual fill aligned with Radix progress semantics.

## Design Notes

- Use determinate values when possible.
- Keep the track visible against the page background.
- Pair progress with a label when completion matters.
