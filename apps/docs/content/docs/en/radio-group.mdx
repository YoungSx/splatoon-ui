export const metadata = {
  title: 'RadioGroup',
  description: 'Single-choice radio group with Splatoon UI indicator styling.',
  category: 'forms',
  example: 'radio-group',
}

## Usage

Use RadioGroup for mutually exclusive choices where every option should stay visible.

## Design Notes

- Pair every item with a visible Label.
- Use Select instead when the option list is long.
