export const metadata = {
  title: 'Section',
  description: 'Layout section primitive with size, pattern, background, and overlay clearance controls.',
  category: 'layout',
  example: 'section',
}

## Usage

Use Section to frame page bands with consistent spacing and published background patterns.

## Design Notes

- Use `size="lg"` for major page regions.
- Reserve overlay clearance when decorative dividers cross the section edge.
