export const metadata = {
  title: 'SegmentedControl',
  description: 'Mutually exclusive segmented choices with button and track appearances.',
  category: 'forms',
  example: 'segmented-control',
}

## Usage

Use SegmentedControl for short option sets that benefit from immediate comparison.

## Design Notes

- Keep the set to a small number of options.
- Use the track appearance for mode toggles that need stronger visual treatment.
