export const metadata = {
  title: 'Select',
  description:
    'Typed select primitive with trigger, value, content, items, groups, and scroll controls.',
  category: 'forms',
  example: 'select',
}

## Usage

Use Select when users choose one value from a compact list.

## Design Notes

- Keep item values stable and serializable.
- Use grouped items only when the grouping helps scanning.
