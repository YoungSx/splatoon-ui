export const metadata = {
  title: 'SplatoonTitle',
  description:
    'Splatoon-style heading primitive with text fallback, image support, sizes, and animation.',
  category: 'typography',
  example: 'splatoon-title',
}

## Usage

Use SplatoonTitle for display headings that should carry the visual language directly.

## Design Notes

- Use text children for accessible fallback text.
- Reserve image titles for authored art assets.
