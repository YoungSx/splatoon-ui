export const metadata = {
  title: 'Switch',
  description: 'Immediate on/off control with image-backed track styling.',
  category: 'forms',
  example: 'switch',
}

## Usage

Use Switch for settings that take effect immediately. Use Checkbox when the value is submitted later as part of a larger form.

## Design Notes

- Keep on/off labels short.
- Use one accent color per settings group.
- Do not hide the accessible label.
