export const metadata = {
  title: 'TapeDivider',
  description: 'Tape-strip divider with primary, double, rotation, and overlap options.',
  category: 'layout',
  example: 'tape-divider',
}

## Usage

Use TapeDivider between page sections that need a Splatoon tape transition.

## Design Notes

- Use `variant="double"` when the boundary needs stronger separation.
- Use overlap only when neighboring sections can absorb the negative margin.
