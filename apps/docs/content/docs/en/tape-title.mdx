export const metadata = {
  title: 'TapeTitle',
  description: 'Short title treatment rendered on a torn tape label.',
  category: 'typography',
  example: 'tape-title',
}

## Usage

Use TapeTitle for compact labels and feature titles that need a tape-backed heading.

## Design Notes

- Keep copy short because the tape ends reserve fixed bleed space.
- Use `color="black"` for the strongest contrast and red or yellow for accent moments.
