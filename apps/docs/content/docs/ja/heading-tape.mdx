export const metadata = {
  title: 'HeadingTape',
  description: '任意の sticker decorations と safe-area sizing を持つ tape-backed heading です。',
  category: 'typography',
  example: 'heading-tape',
}

## 使い方

HeadingTape は tape label treatment が必要な section title に使います。

## 設計メモ

- 狭い surface では `size="compact"` を使います。
- 周囲の内容がすでに密な場合は decorations を無効にします。
