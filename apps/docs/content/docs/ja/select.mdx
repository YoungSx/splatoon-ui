export const metadata = {
  title: 'Select',
  description: 'trigger、value、content、item、group、scroll controls を持つ型付き select primitive です。',
  category: 'forms',
  example: 'select',
}

## 使い方

Select はコンパクトなリストから値を 1 つ選ぶために使います。

## 設計メモ

- item value は安定し、シリアライズできる値にします。
- group はスキャンしやすくなる場合だけ使います。
