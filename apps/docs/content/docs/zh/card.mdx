export const metadata = {
  title: 'Card',
  description: '支持 paper、staple、rugged 和 torn 变体的可组合内容表面。',
  category: 'data-display',
  example: 'card',
}

## 用法

Card 适合需要明确表面、标题、说明和操作区的内容。子组件会在不同变体间保持一致间距。

## 设计备注

- `paper` 用于中性内容。
- `staple` 和 `torn` 用于更强的编辑化表面。
- 避免把完整卡片嵌套进另一张卡片。
