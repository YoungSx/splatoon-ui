export const metadata = {
  title: 'Section',
  description: '布局区块 primitive，支持尺寸、纹理、背景和装饰留白控制。',
  category: 'layout',
  example: 'section',
}

## 用法

Section 用于用一致间距和公开纹理组织页面区块。

## 设计备注

- 主要页面区域使用 `size="lg"`。
- 装饰分隔跨过区块边缘时预留 overlay clearance。
