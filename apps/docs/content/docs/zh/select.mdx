export const metadata = {
  title: 'Select',
  description: '类型化 select primitive，包含 trigger、value、content、item、group 和滚动控件。',
  category: 'forms',
  example: 'select',
}

## 用法

Select 用于从紧凑列表中选择一个值。

## 设计备注

- 保持 item value 稳定且可序列化。
- 只有当分组有助于浏览时才使用 group。
