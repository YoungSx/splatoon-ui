export const metadata = {
  title: 'Sheet',
  description: '抽屉表面，支持 overlay、方向、关闭控件和布局组成件。',
  category: 'feedback',
  example: 'sheet',
}

## 用法

Sheet 用于需要比 Popover 更多空间的次级流程。

## 设计备注

- 根据页面布局选择打开方向。
- 让主页面上下文保留在 overlay 后方。
