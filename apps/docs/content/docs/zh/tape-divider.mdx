export const metadata = {
  title: 'TapeDivider',
  description: '胶带分隔条，支持 primary、double、旋转和 overlap。',
  category: 'layout',
  example: 'tape-divider',
}

## 用法

TapeDivider 用于需要 Splatoon 胶带过渡的页面区块之间。

## 设计备注

- 边界需要更强分隔时使用 `variant="double"`。
- 只有相邻区块能吸收负边距时才启用 overlap。
