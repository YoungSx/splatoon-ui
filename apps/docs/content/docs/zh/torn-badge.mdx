export const metadata = {
  title: 'TornBadge',
  description: '直接使用的撕纸 badge primitive，包含颜色预设和类型化 props。',
  category: 'data-display',
  example: 'torn-badge',
}

## 用法

TornBadge 用于需要原始撕纸 badge，而不需要 Badge 高阶封装的场景。

## 设计备注

- 标签文字保持短。
- 需要库默认 badge 抽象时优先使用 Badge。
